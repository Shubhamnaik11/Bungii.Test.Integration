package com.bungii.common.utilities;

import net.thucydides.core.model.TestStep;
import net.thucydides.core.steps.StepEventBus;

public class CucumberWithSerenityUtility {

   /* public String getStepName()
    {
        if (!StepEventBus.getEventBus().isBaseStepListenerRegistered()) {
            return "Unknown"; // CucumberWithSerenity is required.
        }
        String currentStepDescr;
        currentStepDescr = StepEventBus.getEventBus().getCurrentStep().toString();
        return currentStepDescr;
    }*/
}
